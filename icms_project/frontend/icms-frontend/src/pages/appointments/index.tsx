import { Box, Typography } from '@mui/material';

const Appointments = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Appointments
      </Typography>
      {/* TODO: Implement appointments list and calendar view */}
    </Box>
  );
};

export default Appointments;
